#pragma once
#include <iostream>
template <typename T>
void redundantToReadThisIsJustACodeSnippet()
{
    std::cout << "Oh yeah, here is the catch, you see – some other things can be also included between < and > symbols, simply ignore them" << T{};
}